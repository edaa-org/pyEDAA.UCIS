-r ../requirements.txt

# Coverage collection
Coverage>=6.5

# Test Runner
pytest>=7.1.3
pytest-cov>=4.0.0

# Static Type Checking
mypy>=0.981
lxml>=4.9

# Stubs for typechecking (because lxml is used by the project itself)
lxml-stubs>=0.3.1
