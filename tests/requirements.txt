-r ../requirements.txt

# Coverage collection
Coverage>=6.3

# Test Runner
pytest>=7.0.1
pytest-cov>=3.0.0

# Static Type Checking
mypy>=0.931
lxml>=4.8

# Stubs for typechecking (because lxml is used by the project itself)
lxml-stubs>=0.3.1
